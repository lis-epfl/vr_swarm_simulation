using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class screenPawn : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {


        // Create a quad object, render texture, and screen material to display the camera of each drone
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
